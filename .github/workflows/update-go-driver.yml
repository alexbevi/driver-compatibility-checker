name: Update Go Driver

on:
  schedule:
    # Run daily at staggered times
    - cron: '30 2 * * *'

  workflow_dispatch:
    inputs:
      force_recreate:
        description: 'Force recreate JSON file with all releases'
        required: false
        type: boolean
        default: false

jobs:
  update-go:
    uses: ./.github/workflows/update-single-driver.yml
    with:
      driver_name: 'go'
      driver_display_name: 'Go'
      repo_name: 'mongodb/mongo-go-driver'
      force_recreate: ${{ fromJSON(github.event.inputs.force_recreate || 'false') }}
    secrets: inherit

  notify:
    needs: update-go
    runs-on: ubuntu-latest
    if: always()
    steps:
    - name: Create summary
      run: |
        echo "## 🔄 Go Driver Update Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Repository:** https://github.com/mongodb/mongo-go-driver" >> $GITHUB_STEP_SUMMARY
        echo "**Force Recreate:** ${{ github.event.inputs.force_recreate }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY

        if [ "${{ needs.update-go.result }}" == "success" ]; then
          echo "✅ **Status:** Workflow completed successfully" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Status:** Workflow failed or was cancelled" >> $GITHUB_STEP_SUMMARY
        fi
