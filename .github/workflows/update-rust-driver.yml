name: Update Rust Driver

on:
  schedule:
    # Run daily at staggered times
    - cron: '30 5 * * *'
  
  workflow_dispatch:
    inputs:
      force_recreate:
        description: 'Force recreate JSON file with all releases'
        required: false
        type: boolean
        default: false

jobs:
  update-rust:
    uses: ./.github/workflows/update-single-driver.yml
    with:
      driver_name: 'rust'
      driver_display_name: 'Rust'
      repo_name: 'mongodb/mongo-rust-driver'
      force_recreate: ${{ github.event.inputs.force_recreate == true }}
    secrets: inherit
    
  notify:
    needs: update-rust
    runs-on: ubuntu-latest
    if: always()
    steps:
    - name: Create summary
      run: |
        echo "## 🔄 Rust Driver Update Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Repository:** https://github.com/mongodb/mongo-rust-driver" >> $GITHUB_STEP_SUMMARY
        echo "**Force Recreate:** ${{ github.event.inputs.force_recreate }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "${{ needs.update-rust.result }}" == "success" ]; then
          echo "✅ **Status:** Workflow completed successfully" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Status:** Workflow failed or was cancelled" >> $GITHUB_STEP_SUMMARY
        fi
